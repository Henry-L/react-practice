# EverTrue Coding Project


##Environment Setup
Simply clone the repository, then run:

```
npm install
npm start
```
(this assumes you have NPM setup already)

##Linting
There is currently no real test suite, but you can run JSCS with
```
npm test
```
